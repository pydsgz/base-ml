Installation
============

* Using docker:

    .. code-block:: bash

        $ (sudo) docker pull pydsgz/base_ml:latest

* Using pip:
    .. code-block:: bash

        $ (sudo) pip install -r requirements.txt